<% if (typeof questionType != 'undefined' && questionType == 'text'){ %>
  <%- partial("partials/questions/number_field", { questionText: questionText, transactionKey: "dc" }) %>
<% } else if (typeof questionType != 'undefined' && questionType == 'cost') { %>
<%- partial("partials/questions/cost_field", { questionText: questionText, transactionKey: "cost" }) %>
<% } else { %>
  <%- partial("partials/questions/number_dropdown", { questionText: questionText, transactionKey: "dc" }) %>
<% } %>
