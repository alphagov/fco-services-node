<p>The cost for <%= pluralise("registration", calculation.registrationCount) %> and <%= pluralise("certificate", calculation.documentCount) %><% if (calculation.postage){ %><%= calculation.itemList.postage %><% } %> is £<%= formatMoney(calculation.totalCost) %>.</p>
